百度前端学院-小薇学院-任务3-三栏式布局-两列定宽中间自适应结构
思路：在主要内容区域外加一层div,然后设置父级元素向左浮动，并设置宽度为100%，然后对内容区域设置左右外边距，预留位置给另外两列；
然后给两侧的两列设置向左浮动以及一个合理的左负边距，以使他们可以放置在正确的位置。
最后，要使最外一层容器的高度始终适应子元素中最高的高度，需要对主要内容区域的父级元素清除浮动：
.container:after {
  display:block;
  visibility:hidden;
  font-size:0;
  line-height;0;
  clear:both;
  content:"";
}
